local _ = require("gettext")
return {
    name = "freshrss",
    fullname = _("FreshRSS"),
    description = _([[Browse FreshRSS feeds offline-first (v0.2). Opens your cache immediately, syncs in the background with a progress strip, and queues read/favorite actions when offline.]]),
}
