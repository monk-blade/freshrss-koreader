local _ = require("gettext")
return {
    name = "freshrss",
    fullname = _("FreshRSS"),
    description = _([[Browse FreshRSS offline-first (v0.5.0). Compact branded home (FreshRSS mark, short mode/count title, last-sync clock), denser article list with published dates, feed unread counts, Latin/Gujarati list fonts, icon action bars, tunable image sync (per-article / budget / parallel), HTML viewer with Prev/Unread/Favorite/Next, sync-time image prefetch with relative MuPDF paths, mark-all-as-read, pending-action queue, and Dispatcher hooks. Auto-refresh on open is off by default.]]),
}
